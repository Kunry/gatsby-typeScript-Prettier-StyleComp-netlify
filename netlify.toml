[build]
  publish = "public"
  command = "yarn build"
[build.environment]
  YARN_VERSION = "1.9.4"
  YARN_FLAGS = "--no-ignore-optional"
  NODE_VERSION= "10.10.0"


[[redirects]]
  from = "/level1/*"
  conditions = {Role = ["admin"]}
  status = 200
  force = true